# @package _global_

# train fusion model without wifi
defaults:
  - override /model: fusion_module_nowifi_encoder

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["fusion", "nowifi_encoder"]
  
task_name: "fusion_nowifi_encoder"
