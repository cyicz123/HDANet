# @package _global_

defaults:
  - override /model: fusion_module

task_name: "fusion_baseline"

tags: ["fusion", "baseline"]
logger.tensorboard.name: "fusion_baseline"

# passing checkpoint path is necessary for resizing collage logger images
#ckpt_path: ???

